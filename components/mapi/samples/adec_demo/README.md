Run

```sh
mapi_adec_demo -i wait_decode.raw -o raw_out -C g711u -c 2 -r 8000
```
note: we need to know the  sample rate and channel count about the wait_deocde.wav first

To check decoder result
check the raw_out with cooledit or audacity
