Run

```sh
mapi_aenc_demo -i wait_enc.wav -o out.g711a -C g711a
mapi_aenc_demo  -o out.g711a -C g711a
```
note:if there is no input file,it will bind audio cap

To check encoder result
use music player
